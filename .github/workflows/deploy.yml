- name: Deploy
  uses: appleboy/scp-action@master
  with:
    host: ${{ secrets.AWS_HOST }}
    username: ${{ secrets.AWS_USER }}
    key: ${{ secrets.AWS_SSH_KEY }}
    source: "./"
    target: "/var/www/my-site"
    rm: true  # Видали перед копіюванням
