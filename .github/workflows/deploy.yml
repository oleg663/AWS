name: Deploy with Rsync

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.AWS_SSH_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
    
    - name: Deploy with rsync
      run: |
        rsync -avz \
          --exclude='.git' \
          --exclude='.github/workflows/*.yml' \
          -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no" \
          ./ \
          ${{ secrets.AWS_USER }}@${{ secrets.AWS_HOST }}:/var/www/my-site/
    
    - name: Fix permissions
      run: |
        ssh -i ~/.ssh/id_rsa \
          -o StrictHostKeyChecking=no \
          ${{ secrets.AWS_USER }}@${{ secrets.AWS_HOST }} \
          "sudo chown -R www-data:www-data /var/www/my-site && \
           sudo chmod -R 755 /var/www/my-site && \
           sudo systemctl reload nginx"
    
    - name: Success message
      run: |
        echo "✅ Деплой завершено!"
        echo "📁 Файли: /var/www/my-site/"
        echo "🔗 URL: http://${{ secrets.AWS_HOST }}"
